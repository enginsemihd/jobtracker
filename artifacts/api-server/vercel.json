{
  "buildCommand": "pnpm --filter @workspace/api-zod build && pnpm --filter @workspace/db build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.ts": { "maxDuration": 60 }
  }
}
